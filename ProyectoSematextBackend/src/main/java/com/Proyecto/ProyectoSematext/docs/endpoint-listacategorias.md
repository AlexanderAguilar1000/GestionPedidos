# GET /listacategorias

Devuelve la lista completa de categorías registradas en la base de datos.

- **Controlador:** `ControllerCategoria`
- **Método HTTP:** GET
- **Respuesta:** `200 OK` con un arreglo de categorías (`DTOCategoria`)

No recibe parámetros ni requiere body.
